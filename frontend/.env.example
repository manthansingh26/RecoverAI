# RecoverAI Frontend — Environment Variables

# Backend API base URL
# Leave empty when using Vite's dev server proxy (recommended for local dev)
# Set to full URL when deploying frontend separately
VITE_API_BASE_URL=
